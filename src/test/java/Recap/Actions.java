package Recap;

public class Actions {

    /**
     * ACTIONS
     * ==> To take advanced mouse operations we use actions
     * ex: double click, right click, drag and drop, hover, move mouse cursor
     *
     * Syntax: Actions action=new Actions (driver);
     *
     * UPLOAD AND DOWNLOAD
     * Download: Selenium can not handle download process unless there is a link that can be clicked and downloaded.
     *
     *
     * Assertions===>Verifications
     * 1.assertTrue
     * 2.assertFalse
     * 3.assertEquals
     *
     * SoftAssertion--> type of verification. This assertion doesnn't terminate the execution of test when it fails.
     *                  It simply memorizes and keeps executing and it reports all the
     *
     */
}
